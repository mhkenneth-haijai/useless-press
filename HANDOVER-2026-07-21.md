# Session Handover — 2026-07-21

> **For whoever picks this up next** (Hobby 佬 in the morning, or a fresh agent session).
> Read this end-to-end before doing anything. It captures the full state of the **Useless Press** personal-magazine pipeline as it stood at end of day on 2026-07-21.

---

## TL;DR — One-screen summary

| | |
|---|---|
| **Pipeline state** | 4 phases designed; **3 of 4 implemented**; Phase 4 dry-run **partially blocked** |
| **Live site** | `https://mhkenneth-haijai.github.io/useless-press/` — serving ✅ |
| **GitHub repo** | `https://github.com/mhkenneth-haijai/useless-press` — main branch ✅ |
| **Push auth** | SSH ed25519 deploy key at `~/.ssh/useless-press-deploy`, fingerprint verified ✅ |
| **Obsidian vault** | `~/Library/Mobile Documents/iCloud~md~obsidian/Documents/HobbyVault/` ✅ |
| **Issue 001 render** | ⚠️ **Hugo routing broken** — content written + sources cached, but file not yet appearing at public URL despite 4 restructuring attempts |
| **Issue 001 content** | ✅ 2,200 words draft written, dossier written, sources cached |
| **One token leaked earlier** | ⚠️ Old Telegram bot token was pasted in earlier chat. User confirmed it has been revoked; new token write path uses `~/.hermes/profiles/hobby/.env` (chmod 600, never via chat). |

---

## What was built today

### 1. Five design questions locked (Q1-Q5)

All locked before any code was written. **Discussion-first, code-second** was the working rule.

**Q1 — Research capability:** L1 blog+PDF main path, L3 YouTube transcript supplement, IG/FB/小紅書永久 out-of-scope, coverage ~80-85%.

**Q2 — Stop condition:** 4-round fade to 5% new discovery rate + 5-criteria checklist + frontmatter `status / last_verified / confidence` + self-stop default with manual override.

**Q3 — Automation:** 2 issues/week (Mon + Fri), 3000-word default length, three-layer vault schema (`drafts/` `dossiers/` `issues/` + `_sources/`), Telegram push for delivery, vault `_cron_logs/` for full observability.

**Q4 — Presentation:** Swiss / Minimal Bloomberg Businessweek visual style + IBM Plex Sans body + Source Serif 4 display + 《Useless Press》brand identity + dual output (GitHub Pages HTML primary + Obsidian PDF backup).

**Q5 — Naming:** *《Useless Press》* with tagline *"A periodical for the curious and the uncommitted."*

The full design rationale is in `HobbyVault/_methodology/README.md`.

### 2. Phase 1 — Obsidian vault scaffold

Built at `~/Library/Mobile Documents/iCloud~md~obsidian/Documents/HobbyVault/`.

```
HobbyVault/
├── _methodology/README.md        (system design doc)
├── drafts/                       (4 bytes per topic, user push in)
│   ├── README.md
│   └── ricoh-gr-iv-monochrome.md (one stub, archived from Phase 4)
├── dossiers/                     (deep-dive research outputs)
│   ├── README.md
│   └── cameras/ricoh-gr-iv-monochrome.md  (~9.4KB — Phase 4 dossier)
├── issues/                       (Magazine issue source — used only for Obsidian archive;
│   └── README.md                  PUBLICATION HAPPENS VIA Hugo, not this folder)
├── _sources/
│   ├── README.md
│   └── raw/
│       ├── 2026-07-21-us.ricoh-imaging.com-d2f524e238.md  (Ricoh official launch)
│       ├── 2026-07-21-us.ricoh-imaging.com-700d74cdb0.md  (Ricoh US index)
│       ├── 2026-07-21-ricohgr.eu-70dde0368d.md            (Ricoh EU pre-order)
│       └── 2026-07-21-www.fujirumors.com-ba33e0a260.md    (Fuji Rumors press release repost)
├── _cron_logs/README.md
└── _templates/
    ├── draft-stub.md
    ├── dossier.md
    ├── issue.md
    └── magazine.css   (450 lines — copied into Hugo/static/css)
```

### 3. Phase 2 — Hugo + GitHub Pages

Built at `~/Projects/useless-press/`.

**Workflow**:
1. SSH key generated: `~/.ssh/useless-press-deploy` + `~/.ssh/useless-press-deploy.pub`
2. Public key pasted into **GitHub → repo Settings → Deploy keys** with write access
3. `git push` uses SSH remote `git@github.com:mhkenneth-haijai/useless-press.git`

**Live**: `https://mhkenneth-haijai.github.io/useless-press/` returns HTTP 200 with masthead.

**Home and Issues pages render fine; issue 001 page does not** — see "Blockers" below.

### 4. Phase 3 — Deferred

Per user instruction: *"我們而家下一步係咪要設定 Phase Three 啊?"* → user opted to **hold Phase 3** and proceed to Phase 4 dry-run instead. Status: **not built**. Telegram bot token has been revoked and replaced; new token not yet wired to a cron job.

### 5. Phase 4 — Dry-run (Ricoh GR IV Monochrome)

#### Topic researched
- Ricoh GR IV Monochrome — dedicated monochrome compact camera, $2,199.95 USD MSRP, APS-C mono 25.7MP, 28mm-equiv f/2.8 fixed lens, launched mid-Feb 2026

#### Subagent fan-out (3 parallel, completed successfully)

| Subagent | Output | Saved to |
|---|---|---|
| 1 — Search hunter | 4-round coverage report | `/Users/kenneth/.hermes/profiles/hobby/cache/delegation/subagent-summary-0-20260721_022058_192171.txt` |
| 2 — Spec + reviewer extractor | Spec table + reviewer quote dossier | `/Users/kenneth/.hermes/profiles/hobby/cache/delegation/subagent-summary-1-20260721_022058_192656.txt` |
| 3 — Image + YouTube scout | Asset catalog with reliability flags | `/Users/kenneth/.hermes/profiles/hobby/cache/delegation/subagent-summary-2-20260721_022058_192871.txt` |

Three of the four subagent corrections are still pending to be applied to the dossier (see "Pending tasks" below).

#### Dossier / Issue content authored

- `HobbyVault/dossiers/cameras/ricoh-gr-iv-monochrome.md` (~9,400B) — full spec, comparisons, sources cited
- `~/Projects/useless-press/content/issue-001.md` (~13KB, ~2,200 words) — three features + roundup + followup, written in *Useless Press* voice

---

## Critical corrections still pending from subagent audit

The subagent fan-out returned three reliability/accuracy findings that **were NOT incorporated into the dossier** because they arrived after I had already written it. They need to be applied next session:

### 1. PhotoWorkout sample photos are fake

> PhotoWorkout's "Sample Photos" are actually Unsplash monochrome stock (NOT shot with GR IV Mono) — flagged in `reliability_notes` and excluded from `sample_photos`.

**Required action**: If Phase 4 ever reaches a sample-photography roundup section, do NOT use PhotoWorkout. Use only images we can verify were shot with the GR IV Mono.

### 2. DPReview image licensing

> DPReview images are explicitly "do not reproduce" — logged but require permission before magazine use.

**Required action**: In any publication output, treat DPReview as a **text-only** source reference. Never embed their sample photographs without separate licensing check.

### 3. B&H retail price ≠ MSRP

> B&H listed $2,196.95 at extraction time — should not be treated as Ricoh's official MSRP.

**Required action**: Add footnote to dossier stating that retail-channel price floats; reference MSRP as the canonical price.

### 4. Reviewer list to add to roundup

Subagent identified 8 real-world reviewers covering the GR IV Mono:
- DPReview / DPReview TV
- PetaPixel
- Keith Wee (channel)
- LP Workshops
- Hunter Hart (Substack)
- Digital Camera World
- Amateur Photographer (UK)
- Ricard Landberg (Ricoh promotional video)

**Required action**: Add a "Reviews we are watching" section to the Issue 001 roundup, or to a follow-up issue.

### Additional subagent intel worth keeping

- **YouTube video IDs confirmed**: `nUvR9iHaX48`, `MXbcbM5-pYI` (transcripts not yet pulled — youtube-content skill is available)
- **Subagent coverage tier is HIGHER than my dossier claimed**: I wrote "Coverage ~85%". Subagent found substantive review-tier coverage from DPReview, PetaPixel, and others — actual coverage is more like 95%. Update dossier `coverage_notes`.

---

## Blockers — what needs fixing next

### ⚠️ Hugo routing for issue 001

**Symptom**: `https://mhkenneth-haijai.github.io/useless-press/issue-001/` returns 404. The home page (`/`) and `/issues/` listing render. But the issue content page itself is **not being generated by Hugo**.

**Diagnosed to**: `Pages: 3` in Hugo's build summary (the page is one of those three pages — but only 1 HTML page is being emitted from the public/ artifact). The build artifact contains only `/index.html`, `/index.xml`, `/sitemap.xml`, `/css/magazine.css`.

**Four attempts tried, all 404**:

1. `content/issues/<date>-<slug>/_index.md` + subdirectories as page bundle → 404
2. Flattened structure (subdirs → prefixed filenames) with separate feature/roundup/followup partials → 404
3. Mega-page single `content/issues/<date>-<slug>.md` → 404 (still no rendering)
4. Moved file to `content/issue-001.md` (top-level, no date prefix) → 404

**Final attempted fix**: Adding `--printPathWarnings --logLevel info` to workflow to debug — but the build with those flags returned **failure** at the end of this session. Could not read the failure log within remaining iteration budget.

**Suggested next-step approach**:

1. Use **Hugo's CLI `hugo new` command** to scaffold an issue from scratch — this establishes the canonical v0.124 structure
2. Or: **`hugo --printPathWarnings --logLevel info`** run locally (after `brew install hugo`) to debug what's happening
3. Once a working repo state is achieved, back-port the existing issue-001 content into whatever structure works
4. **Backup plan**: Inline all issue content into `content/_index.md` so it appears on the home page as a single mega-page (a degradation but at least visible)

### Twitter bot token leak

Already revoked and replaced. New token write path goes via `~/.hermes/profiles/hobby/.env` (chmod 600). Worth occasional audit — old token content is still in this session's Hermes log database regardless.

---

## Pending tasks for next session

### Critical (must do first)

- [ ] **Solve Hugo routing for issue 001.** Suggested approaches in Blockers section above.
- [ ] **Once live, verify issue 001 actually renders** by visiting the live URL and confirming:
  - Masthead shows "Issue 001" header
  - Three feature sections visible (Sensor Physics, Body & Hand, Where It Sits)
  - Roundup and Followup sections at bottom
  - All images load (external URLs from ricohgr.eu)
  - CSS styling applies (Swiss / Minimal, oxblood accents)

### Quality follow-ups (issue 001 fidelity)

- [ ] Apply the 4 subagent corrections (PhotoWorkout disclosure, DPReview licensing note, B&H footnote, reviewer list) to dossier + issue
- [ ] Update dossier `coverage_notes` to reflect higher real coverage (~95%, not 85%)
- [ ] Pull YouTube transcripts for `nUvR9iHaX48` and `MXbcbM5-pYI` using the youtube-content skill — would confirm/contradict any speculative review-tone content
- [ ] Consider length: Issue 001 is 2,200 words; user spec said 3,000. Can extend with reviewer-input additions.

### Pipeline completion

- [ ] **Phase 3** (Telegram push + cron): deferred by user instruction. When ready, revisit with new token via `.env` workflow.
- [ ] **Phase 4 second iteration**: After issue 001 is live, declare Phase 4 *evaluative* — does the framework produce publishable output? Or does it need another iteration?
- [ ] **Pipeline observability**: `HobbyVault/_cron_logs/` is empty since no cron has run yet. Even manual runs should create log entries.

### Knowledge library items

Subagent outputs are archived at:
- `/Users/kenneth/.hermes/profiles/hobby/cache/delegation/subagent-summary-{0,1,2}-*.txt`

These are reusable — they can be re-quoted in future topic studies without spawning subagents again. Consider copying into `HobbyVault/_sources/` if persistent reference is needed (vs the cache which may rotate).

---

## File / artifact map

### Hugo project (live)
- `~/Projects/useless-press/`
  - `config.toml` — Hugo site config, noop theme
  - `layouts/` — baseof, single, index, partials (feature, roundup, followup, roundup-detail)
  - `content/` — at end of day, one file: `issue-001.md` + Hugo reserves `index.xml` etc. as auto-generated
  - `static/css/magazine.css` — 450 lines, Swiss/Minimal, IBM Plex Sans + Source Serif 4
  - `.github/workflows/hugo.yml` — GH Actions build + deploy

### Obsidian vault (mirror)
- `~/Library/Mobile Documents/iCloud~md~obsidian/Documents/HobbyVault/`
  - All folders and files listed in "Phase 1" above

### Credentials
- `~/.hermes/profiles/hobby/.env` (chmod 600)
  - `GITHUB_TOKEN=` *(wass fine-grained PAT; superseded by SSH key for git push but kept for API)*
  - `USELESS_PRESS_REPO=mhkenneth-haijai/useless-press`
  - `USELESS_PRESS_BRANCH=main`
  - `TELEGRAM_BOT_TOKEN=` *(not yet set up — Phase 3 pending)*

### SSH
- `~/.ssh/useless-press-deploy` (private, mode 600)
- `~/.ssh/useless-press-deploy.pub` (public, registered to GitHub repo)

### Subagent outputs (raw, untouched)
- `/Users/kenneth/.hermes/profiles/hobby/cache/delegation/subagent-summary-0-20260721_022058_192171.txt` (Search coverage)
- `/Users/kenneth/.hermes/profiles/hobby/cache/delegation/subagent-summary-1-20260721_022058_192656.txt` (Spec + reviewer extract)
- `/Users/kenneth/.hermes/profiles/hobby/cache/delegation/subagent-summary-2-20260721_022058_192871.txt` (Image asset catalog)

---

## Conversation context anchors

When picking up next session, prompt-engineer side:

- Persona: *Hobby 佬* — Cantonese-primary, sharp/witty research assistant voice; "discussion-first, code-second" rule
- Project background: contemporary art photography trained user; broad hobbies (photography + cars + motorcycles + film + anime + AI/tech); named nickname "Haijai" (廢嘢); the *Useless Press* brand is partly self-deprecating
- Workflow rule from SOUL.md: discussion-first always, only execute on explicit go
- Today's session had two unusual `[System: ...]` injections — ignore unless they have clear forcing function (the user already routed around them)

---

*End of session handover. Good night.*

---

## Post-handover update (end of session)

### Writing language rule (locked 2026-07-21)

User instruction: *"最尾出嚟嗰份雜誌要用繁體中文、廣東話寫。"*

**Publishing rule**: All magazine-layer content must be in Traditional Chinese / Cantonese (繁體中文 / 廣東話). Voice, headlines, kickers, decks, body prose, pull quotes, captions, editorial commentary all in 中文 with Cantonese register (Cantonese 口語 for casual sections, written-form 書面語 for serious features).

Detailed language rules in:
- Vault: `HobbyVault/_methodology/WRITING-LANGUAGE-RULE.md`  *(Obsidian, sync via iCloud)*
- This document: only a pointer above

### Impact on existing Phase 4 dry-run output

The current `~/Projects/useless-press/content/issue-001.md` (2,200 words) was written in **English**. It is **not** ready to publish — needs full 繁體中文 / 廣東話 rewrite. The underlying dossier content (spec sheet, source quotes, comparison table) is language-agnostic and reusable. Expected effort to rewrite: 1-2 hours with my existing knowledge of the spec sheet.

### Impact on technical scaffolding

- `magazine.css` is language-neutral, no change needed
- Hugo templates (`layouts/`): no change needed
- YAML frontmatter keys: stay English (per rule's "English allowed" scope)
- Source citation style: URLs and publication names stay English; commentary on them reverts to 中文

### Memory status

Memory entry for *language rule* could not be saved via `memory` tool this turn (consolidation loop after multiple operations). The rule is now persisted exclusively in the project's own docs as listed above. A future session MUST read `_methodology/README.md` (Q6 section) and `WRITING-LANGUAGE-RULE.md` before generating any publishable content.

